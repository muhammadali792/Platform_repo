name: "EKS Infrastructure Pipeline"

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  terraform_deploy:
    name: "Terraform Apply"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "latest"

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
            -backend-config="key=cloudaura/terraform.tfstate" \
            -backend-config="region=${{ vars.AWS_REGION }}" \
            -backend-config="use_lockfile=true"

      - name: Terraform Format Check
        run: terraform fmt -check

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Apply (Route53 Zone First)
        run: terraform apply -auto-approve

      # 💡 Senior Tip: Agar outputs dekhne hain toh formal standard yeh hai:
      # - name: Show Nameservers
      #   run: terraform output name_servers

      # Full apply tab run karo jab nameservers domain registrar pe update ho jayein
      # - name: Terraform Apply (Full)
      #   run: |
      #     terraform apply \
      #       -var="domain_name=${{ vars.DOMAIN_NAME }}" \
      #       -auto-approve
